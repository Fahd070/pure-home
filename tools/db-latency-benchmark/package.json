{
  "name": "db-latency-benchmark",
  "private": true,
  "version": "1.0.0",
  "description": "Temporary, standalone Singapore-vs-Oregon Supabase latency benchmark. Not part of the Pure Home application. No routes, no auth, no cron, no writes -- GET /health performs only `SELECT 1`.",
  "main": "index.js",
  "scripts": {
    "build": "prisma generate --schema=prisma/schema.prisma",
    "start": "node index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
